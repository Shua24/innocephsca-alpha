# Use LF line endings; do not normlize to CRLF
* text=auto eol=lf

# Binary assets must never be treated as text (prevents PNG corruption)
*.png binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary

# Recognize PatEL files for GitHub language stats
*.ptl linguist-language=PatEL
